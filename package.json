{
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "clean": "turbo clean",
    "component": "cd packages/ds && bunx shadcn-ui add",
	"db": "cd packages/services && bun ",
	"storybook": "cd packages/ds && bun storybook",
    "postinstall": "symlink-dir ./public ./apps/app/public"
  },
  "prisma": {
    "schema": "packages/database/prisma/schema.prisma",
    "seed": "tsx packages/database/src/seed.ts"
  },
  "devDependencies": {
    "@repo/config": "*",
    "eslint": "^8.57.0",
    "tsx": "^4.7.1",
    "turbo": "latest"
  },
  "packageManager": "bun@1.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "symlink-dir": "^5.2.1"
  }
}
